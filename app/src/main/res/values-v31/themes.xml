<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema utama TemuCafe -->
    <style name="Theme.TemuCafe" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Splash Screen Android 12+ -->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="android:windowSplashScreenBackground">@color/brown_700</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/brown_900</item>

        <!-- Styling tambahan -->
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:fontFamily">@font/montserrat_bold</item>

        <!-- Tambahan kompatibilitas -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

</resources>
